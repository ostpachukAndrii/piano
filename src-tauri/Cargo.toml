[package]
name = "piano-tauri-backend"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "piano-tauri-backend"
path = "src/main.rs"

[lib]
name = "piano_tauri_backend"
path = "src/lib.rs"

[dependencies]
# Tauri
tauri = { version = "2.0", features = [] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"

# Database
sqlx = { version = "0.7", features = ["runtime-tokio-rustls", "sqlite"] }

# Async runtime
tokio = { version = "1.35", features = ["full"] }

# MIDI
midir = "0.9"

# Error handling
thiserror = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = "0.3"

# UUID
uuid = { version = "1.0", features = ["v4", "serde"] }

# Configuration
config = "0.14"
directories = "5.0"

# MusicXML (MXL) support
zip = "2.2"
roxmltree = "0.20"

# Reuse domain models and lessons
piano-domain = { path = "../crates/piano-domain" }
piano-lessons = { path = "../crates/piano-lessons" }

[build-dependencies]
tauri-build = { version = "2.0", features = [] }
