// Tauri playback commands wrapper
// TODO: Implement wrappers for:
// - invoke_get_playback_state(lesson_id: String) -> Future<Result<PlaybackState>>
// - invoke_seek_to_note(lesson_id: String, note_index: usize) -> Future<Result<PlaybackState>>
